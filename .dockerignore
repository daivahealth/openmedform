.git
.github
# NOTE: docs/ is deliberately NOT ignored. The web app renders the published
# subset of it at /docs, and that happens at build time, so the markdown must
# be present in the build context.
**/node_modules
**/.next
**/dist
packages/*/lib
**/.turbo
**/coverage
*.log
.env
**/.env
apps/api/.env
pgadmin
scripts
