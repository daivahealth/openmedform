{
  "name": "@openmedform/form-schema-types",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-independent contract types for the dual-engine form platform (Data Schema / UI Schema / Print Schema / FormDefinition / FormInstance).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "eslint src/",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
