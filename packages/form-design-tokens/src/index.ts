/**
 * @openmedform/form-design-tokens
 *
 * Shared visual/layout tokens for cross-framework rendering parity. Import the
 * TS constants for computed styles, `cssVariables`/`toCssText` to inject the
 * custom properties, or `@openmedform/form-design-tokens/tokens.css` directly.
 */

export * from './tokens';
export * from './css';
